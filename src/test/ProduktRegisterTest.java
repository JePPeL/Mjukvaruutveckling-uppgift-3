package test;

import static org.junit.Assert.*;

import org.junit.Test;

import main.*;

public class ProduktRegisterTest { /*
		Produktregister pr = new Produktregister();
		Produkt p = new Produkt("X2000", "Tåg", "500");

		@Test
		public void testAddAndGet() {
			pr.add(new Produkt("X2000", "Inte tåg", 1000));
			pr.add(p);
			assertEquals(p, pr.get("x2000"));
			assertEquals(null, pr.get("snälltåget"));
			//antar att det ska skrivas över vid konflikt. Om den gamla kunden skall vara kvar istället, lägg till p innan det andra tåget.
		}
		@Test
		public void testRemove() {
			kr.add(k);
			assertEquals(p, pr.get("X2000"));
			assertEquals(k, kr.remove("X2000"));
			assertEquals(null, kr.get("X2000"));
		}
*/
	}
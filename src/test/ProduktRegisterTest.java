package test;

import static org.junit.Assert.*;

import org.junit.Test;

import main.*;

public class ProduktRegisterTest { /*
		Productregister pr = new Productregister();
		Product p = new Product("X2000", "Tåg", "500");

		@Test
		public void testAddAndGet() {
			pr.add(new Product("X2000", "Inte tåg", 1000));
			pr.add(p);
			assertEquals(p, pr.get("x2000"));
			assertEquals(null, pr.get("snälltåget"));
			//Assumes the product should be overwritten in case of conflict, if it should stay the same, add p before the other product.
		}
		@Test
		public void testRemove() {
			pr.add(p);
			assertEquals(p, pr.get("X2000"));
			assertEquals(k, kr.remove("X2000"));
			assertEquals(null, kr.get("X2000"));
		}
*/
	}
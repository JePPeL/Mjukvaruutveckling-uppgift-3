package test;

import static org.junit.Assert.*;

import org.junit.Test;

import main.*;

public class CustomerTest {
	/*Customer c = new Customer("123", "Bert Bertsson", "Smågatan 99");
	Order o = new Order("987");
	
	@Test
	public void testAddAndGetOrder() {
		c.addOrder(new Order("987"));
		c.addOrder(o);
		assertEquals(o, c.findOrder("987"));
		assertEquals(null, c.findOrder("000"));
		//Assumes the order should be overwritten in case of conflict, if it should stay the same, add o before the other order.
	}
	@Test
	public void testRemoveOrder() {
		c.addOrder(o);
		assertEquals(o, c.findOrder("987"));
		assertEquals(o, c.removeOrder("987"));
		assertEquals(null, c.findOrder("987"));
	}
	@Test
	public void testChangingCustomer() {
		c.setName("Anders Andersson");
		c.setAddress("Storgatan 1");
		assertEquals("Anders Andersson", c.getName());
		assertEquals("Storgatan 1", c.getAddress());
	}
*/
}